# SEO Implementation

This project uses the Next.js App Router metadata API for page-level SEO.

- Root metadata is defined in `src/app/layout.tsx`
- Additional route metadata can be added with segment-level `metadata` exports
- Images can be optimized via `next/image`

